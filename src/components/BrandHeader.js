import React from "react";
import styled from "styled-components";

const Logo = styled.div`
  font-size: 1.5rem;
  font-weight: bold;
`;

const BrandHeader = () => <Logo>Logo</Logo>;

export default BrandHeader;
